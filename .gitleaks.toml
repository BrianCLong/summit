[rules]

[allowlist]
  description = "known false positives and test fixtures"
  paths = [
    "tests/fixtures/",
    "test-data/",
    "testdata/",
    "**/*.example",
    "**/*.sample",
    "**/fixtures/**"
  ]
