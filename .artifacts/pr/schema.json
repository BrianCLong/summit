{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PR Evidence Artifact",
  "description": "Preserves unused work from superseded or closed PRs to maintain evidence-first governance.",
  "type": "object",
  "properties": {
    "pr": {
      "type": "integer",
      "description": "The pull request number."
    },
    "concern": {
      "type": "string",
      "description": "The concern classification for the PR."
    },
    "patch_hash": {
      "type": "string",
      "description": "SHA-256 hash of the patch content."
    },
    "superseded_by": {
      "type": ["integer", "string"],
      "description": "The canonical branch or PR that superseded this work."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "status": {
      "type": "string",
      "enum": ["superseded", "merged", "quarantined", "abandoned"]
    },
    "message": {
      "type": "string"
    }
  },
  "required": ["pr", "concern", "patch_hash", "status", "timestamp"]
}
