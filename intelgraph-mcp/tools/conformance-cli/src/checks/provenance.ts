export async function run(_ctx: { endpoint: string; token?: string }) {
  // Placeholder: inspect provenance hashes and retention metadata.
  return { name: 'provenance', pass: false, reason: 'ledger-integration-pending' };
}
