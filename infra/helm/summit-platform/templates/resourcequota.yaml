{{- if .Values.global.resourceQuota.enabled }}
apiVersion: v1
kind: ResourceQuota
metadata:
  name: {{ .Values.global.resourceQuota.name }}
  namespace: {{ .Values.global.namespace | default .Release.Namespace }}
spec:
  hard:
{{ toYaml .Values.global.resourceQuota.hard | indent 4 }}
{{- end }}
