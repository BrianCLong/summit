{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Cognitive Targeting Engine Configuration Schema",
  "description": "Schema for validating the config.json file of the Cognitive Targeting Engine.",
  "type": "object",
  "properties": {
    "emotion_to_bias": {
      "type": "object",
      "patternProperties": {
        "^[a-zA-Z_]+$": {
          "type": ["string", "null"]
        }
      },
      "additionalProperties": false,
      "description": "Mapping of emotional states to cognitive bias profiles."
    },
    "bias_to_vectors": {
      "type": "object",
      "patternProperties": {
        "^[a-zA-Z_]+$": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false,
      "description": "Recommended influence vectors per bias profile."
    }
  },
  "required": ["emotion_to_bias", "bias_to_vectors"],
  "additionalProperties": false
}
