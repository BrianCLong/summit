import { WorldState } from "../../graphrag/world_model/state_model";
import { AgentAction } from "../world_model/action_space";

export function simulatePlan(
  state: WorldState,
  actions: AgentAction[]
) {
  // rollout simulation
  // evaluate outcomes
  // select best policy
  return state;
}
