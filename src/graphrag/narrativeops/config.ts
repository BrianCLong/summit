export const NARRATIVEOPS_FLAGS = {
  enabled: false,
  extractAssumptions: false,
  stateMachine: false,
} as const;

export const NARRATIVEOPS_TRANSFORM = {
  name: 'narrativeops-evidence',
  version: '0.1.0',
} as const;
