# Policy Gateway

Central service exposing policy evaluation for releasability, egress, and incident triggers.

- Accepts scope tokens, jurisdictional policies, and control catalogs.
- Provides decisions for redaction rules, egress allowlists, incident trigger mappings.
- Emits decision transparency logs and replay tokens for downstream artifacts.
