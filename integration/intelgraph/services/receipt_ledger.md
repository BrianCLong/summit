# Receipt Ledger

Append-only ledger storing receipt hashes for origin certificates, collision proofs, migration proofs, macro artifacts, and linkage receipts.

## Capabilities
- Hash-chaining entries for tamper evidence.
- Stores replay tokens, attestation status, and budget parameters.
- Provides audit queries and export with redaction per policy gateway.
