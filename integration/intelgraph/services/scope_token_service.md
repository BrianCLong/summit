# Scope Token Service

Issuance and validation of classification scope tokens for coalition operations.

- Issues signed tokens with bindings (tenant, purpose, scope, TTL, policy version).
- Validates tokens for enforcement layers; exposes status endpoint.
- Logs issuance/validation events to transparency log for audit.
