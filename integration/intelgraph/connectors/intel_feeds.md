# Intel Feeds Connector

## Purpose

Normalize threat-intel feeds (IOCs, TTPs, advisories) into evidence bundles.

## Requirements

- Capture source trust scores and freshness metadata.
- Enforce policy-based redaction before export.
- Provide determinism tokens for lifecycle computations.
