# Ontology View Manager UI

Manages compiled ontology views and ABIs for partners.

## Features

- Display permitted types, fields, and actions per policy scope.
- ABI compatibility checks with version mapping visuals.
- Export view artifacts with witness and attestation references.
