# UI Spec: Egress Receipt Audit View

- Show egress receipts with destination categories, byte counts, and halt events.
- Display compliance decision and policy identifiers; indicate downgrade markers when attestation absent.
- Provide transparency log inclusion proofs and replay token export for auditors.
