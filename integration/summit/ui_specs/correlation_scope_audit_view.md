# UI Spec — Correlation Scope Audit View

## Purpose
Audit LLCT correlation token enforcement and linkage receipts.

## Layout
- Token scope summary (identifier types, hop count, TTL, egress limit, jurisdiction).
- Execution timeline showing module outputs and blocked/redacted links.
- Linkage receipt ledger with hash-chain pointers and attestation status.
- Egress usage meter with redaction events.
- Replay controls to revalidate enforcement using replay token.
