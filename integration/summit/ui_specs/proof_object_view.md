# UI Spec: Proof Object View

- Display proof object summary (plan hash, policy decision ID, determinism token).
- Show disclosure obligations applied and any counterfactual variants with information loss metrics.
- Provide verification status (signature, attestation, transparency inclusion) and download link.
