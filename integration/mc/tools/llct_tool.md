# LLCT Tool

CLI enforcing linkage-limited correlation tokens for OSINT runs.

## Usage
- `mc llct execute --token token.json --modules modules.json`

## Outputs
- Selective results and linkage receipt containing linked identifiers, token hash, hop/egress usage, replay token, optional attestation quote.
