# MC Tool: tool_assurance

- Requires connector package attestation and SBOM validation via SCAF API before execution.
- Enforces execution policy locally (destinations/effects) and logs egress receipts.
- Returns assurance report commitment, egress receipt, and attestation validation token.
