# QSDR Tool (MC)

CLI for safe recon execution with canary monitoring.

## Commands

- `qsdr run --targets t1,t2 --modules m1,m2`
- `qsdr audit --id <killAuditId> --include-proof`

## Outputs

- Recon results up to halt point, kill audit IDs, policy decision IDs, and replay tokens.
