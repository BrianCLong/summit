# PGTT Tool

CLI for generating governance regression suites and reports.

- Inputs: policy version, ontology version, execution budget, seed.
- Outputs: governance test report, regressions, explanation artifact, replay token, commitments.
- Modes: `--changed-rules` to focus counterfactual tests on modified policy rules.
