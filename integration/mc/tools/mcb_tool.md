# MCB Tooling

CLI for executing metric contracts, generating benchmark binders, and exporting evaluator summaries.

- Runs scoring pipelines with deterministic seeds and records replay tokens.
- Supports drift checks and counterfactual binder generation.
