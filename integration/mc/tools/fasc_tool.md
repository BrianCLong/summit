# FASC Tool (MC)

CLI for online calibration management and drift inspection.

## Commands

- `fasc calibrate --window 24h --feeds feedA,feedB`
- `fasc drift --feed feedA --window 7d`

## Outputs

- Calibration artifacts with feed weights, drift indicators, justification witness IDs, and replay tokens.
