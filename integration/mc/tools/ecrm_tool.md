# ECRM Tooling

CLI for launching scoped OSINT runs inside sandboxes and producing tier manifests.

- Validates scope tokens, monitors egress receipts, and enforces budgets.
- Exports tiered outputs and logs transparency digests for evaluator consumption.
