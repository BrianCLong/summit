# PQLA Tool (MC)

CLI to run policy-qualified analytics locally or on partner nodes.

## Commands

- `pqla run --query "MATCH ..." --purpose research --export-token token123`
- `pqla artifact --id <artifactId> --include-proof`

## Outputs

- Sanitized analytics results, compliance artifact IDs, disclosure summaries, and determinism tokens.
