# TCGI Tool

CLI to synthesize macros from induced grammar.

## Usage
- `mc tcgi synthesize --objective objective.json --constraints constraints.json`

## Outputs
- Macro artifact with macro graph, justification mapping to production rules, expected cost, replay token, optional attestation quote.
