# Guardrail: Budget Enforcer

- Enforce maximum runtime and egress byte budgets per request.
- Emit budget violation events with determinism tokens.
- Degrade to safe defaults when limits are reached.
