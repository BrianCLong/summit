# Verify-First Guardrail

Never pass unverified evidence to the LLM or downstream tools. All artifacts must include verification receipts from `POST /v1/evidence/verify` and reference policy decision tokens.
