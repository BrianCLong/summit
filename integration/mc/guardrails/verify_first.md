# Verify-First Guardrail

Ensure artifacts are verified before downstream automation.

## Rules

- Require valid replay token and commitment root.
- Enforce attestation verification when configured.
- Block execution if verification fails or is missing.
