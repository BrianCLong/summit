# Guardrail: Attestation Required

Tools that execute code or transforms must require attestation.

## Requirements

- Verify template or runtime measurement hashes.
- Validate signer identity against trusted list.
- Emit compliance log entry for attestation failures.
