# Rollback Templates

This directory contains templates for generating rollback scripts for dangerous operations (e.g., unbinding VFIO devices).
Summit never applies these automatically; they are provided for the user to review and execute if needed.
