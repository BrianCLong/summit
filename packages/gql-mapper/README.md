# GQL Mapper

A utility to map a subset of ISO/IEC 39075 GQL to Cypher for Summit's workload.

## Usage

Use `make gql-compat` to generate the support matrix artifact `artifacts/gql-support-matrix.json`.
