{
  "name": "@intelgraph/memory",
  "version": "0.1.0",
  "description": "Persistent memory layer for Summit AI chat flows",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest --runInBand --config ../../jest.config.cjs --runTestsByPath packages/memory/__tests__"
  },
  "dependencies": {},
  "devDependencies": {}
}
