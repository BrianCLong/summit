{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FactCert Cryptographic Stamp",
  "type": "object",
  "properties": {
    "version": {
      "type": "string",
      "const": "1.0.0"
    },
    "hash": {
      "type": "string",
      "description": "SHA-256 hash of the canonicalized credential payload"
    },
    "signature": {
      "type": "string",
      "description": "Ed25519 signature of the hash (hex encoded)"
    },
    "publicKey": {
      "type": "string",
      "description": "Public key used to verify the signature (PEM or hex)"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when the stamp was created"
    }
  },
  "required": ["version", "hash", "signature", "publicKey", "timestamp"]
}
