{
  "name": "@intelgraph/prov-ledger-verifier",
  "version": "1.0.0-rc.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "fixtures"
  ],
  "bin": {
    "prov-ledger-verify": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest --passWithNoTests",
    "verify:fixtures": "pnpm run build && node dist/cli.js --bundle fixtures/valid-bundle --json && node dist/cli.js --bundle fixtures/valid-bundle.zip --json"
  },
  "dependencies": {
    "adm-zip": "^0.5.12"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.6",
    "@types/jest": "^30.0.0",
    "@types/node": "^22.10.1",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.5",
    "typescript": "^5.9.3"
  }
}
