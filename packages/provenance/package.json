{
  "name": "@intelgraph/provenance",
  "version": "0.1.0",
  "description": "Receipt schema types and helpers for provenance receipts",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "jest --config ../../jest.config.cjs --runTestsByPath packages/provenance/tests"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.10.0",
    "jest": "^29.7.0",
    "typescript": "^5.3.3"
  }
}
