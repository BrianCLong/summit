{
  "name": "@intelgraph/dpec",
  "version": "1.0.0-rc.1",
  "description": "Deterministic Prompt Execution Cache with attestable artifacts and client adapters",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "lint": "eslint . --ext .ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "eslint": "^9.39.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.9.2",
    "vitest": "^2.1.4"
  }
}
