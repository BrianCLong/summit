// Re-export from stream-processing for convenience
export { count, sum, average, min, max, collectList, collectSet, topK, movingAverage, percentile, distinctCount, groupBy, reduce } from '@intelgraph/stream-processing';
