apiVersion: v1
kind: Service
metadata:
  name: liquid-nano
  labels:
    app: liquid-nano
spec:
  type: ClusterIP
  selector:
    app: liquid-nano
  ports:
    - name: http
      port: 80
      targetPort: 8080
