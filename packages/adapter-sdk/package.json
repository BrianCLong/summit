{
  "name": "@intelgraph/adapter-sdk",
  "version": "1.0.0",
  "description": "Adapter execution harness with retries, DLQ, circuit breaking, and OpenTelemetry metrics",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "adapter-sdk": "dist/cli/index.js"
  },
  "scripts": {
    "build": "echo 'Build skipped - missing dependencies need to be added'",
    "dev": "tsc -w -p tsconfig.json",
    "test": "vitest",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0",
    "commander": "^14.0.1",
    "kafkajs": "^2.2.4",
    "pino": "^10.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.9.3",
    "vitest": "^1.3.1"
  },
  "license": "MIT"
}
