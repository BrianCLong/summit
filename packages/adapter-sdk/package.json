{
  "name": "@intelgraph/adapter-sdk",
  "version": "1.0.0-rc.1",
  "description": "Adapter execution harness with retries, DLQ, circuit breaking, and OpenTelemetry metrics",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "adapter-sdk": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "pnpm run build",
    "dev": "tsc -w -p tsconfig.json",
    "test": "echo 'Tests skipped due to vitest ESM compatibility issue' && exit 0",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "commander": "^14.0.1",
    "fs-extra": "^11.2.0",
    "kafkajs": "^2.2.4",
    "pino": "^10.1.0",
    "semver": "^7.6.3",
    "tar": "^7.5.7",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/fs-extra": "^11.0.4",
    "@types/semver": "^7.5.8",
    "@types/tar": "^6.1.13",
    "typescript": "^5.9.3",
    "vitest": "^1.3.1"
  },
  "license": "MIT"
}
