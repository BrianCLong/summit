{
  "name": "webhook-sink",
  "version": "0.1.0",
  "description": "Basic webhook sink adapter",
  "entrypoint": "index.ts",
  "capabilities": ["webhook"],
  "requiredPermissions": ["adapter:webhook:emit"],
  "claims": [],
  "compatibility": { "companyOs": ">=0.1.0" },
  "configSchema": {
    "type": "object",
    "properties": {
      "endpoint": { "type": "string" },
      "secret": { "type": "string" }
    },
    "required": ["endpoint"]
  },
  "signature": "placeholder-signature"
}
