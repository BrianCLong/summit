version: "3"
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: db
  neo4j:
    image: neo4j:5
    environment:
      NEO4J_AUTH: neo4j/password
