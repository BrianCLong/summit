name: Forbidden Secrets Fixture
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Use registry token
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          CODEARTIFACT_AUTH_TOKEN: ${{ secrets.CODEARTIFACT_AUTH_TOKEN }}
        run: echo "Should be flagged"
