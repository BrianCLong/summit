name: Policy Checks
on: [push, pull_request]

jobs:
  opa-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup OPA
        uses: open-policy-agent/setup-opa@v2
        with:
          version: v0.61.0
      - name: Run OPA Tests
        run: opa test packages/policy-engine/opa/ -v
