name: IntelGraph Python CI
on:
  push:
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: pychanges
        continue-on-error: true
        with:
          filters: |
            python:
              - 'python/**'
      - name: Skip (no python changes)
        if: (((((((steps.pychanges.outputs.python != 'true') && (steps.pychanges.outputs.python == 'true')) && (steps.pychanges.outputs.python == 'true')) && (steps.pychanges.outputs.python == 'true')) && (steps.dpchanges.outputs.datap != 'true')) && (steps.dpchanges.outputs.datap == 'true')) && (steps.dpchanges.outputs.datap == 'true')) && (steps.dpchanges.outputs.datap == 'true')
        run: echo "No python/ changes"
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - name: Install (editable with extras)
        working-directory: python
        run: |
          python -m pip install -U pip setuptools wheel
          pip install -e ".[dev,test]"
      - name: Tests
        working-directory: python
        continue-on-error: true
        run: pytest -q
  data-pipelines:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: dpchanges
        continue-on-error: true
        with:
          filters: |
            datap:
              - 'server/data-pipelines/**'
      - name: Skip (no data-pipelines changes)
        run: echo "No data-pipelines changes"
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - name: Install
        working-directory: server/data-pipelines
        run: |
          python -m pip install -U pip
          pip install fastavro aiokafka tenacity pytest pytest-cov
      - name: Tests
        working-directory: server/data-pipelines
        run: pytest tests/test_kafka_consumer_avro.py -q
