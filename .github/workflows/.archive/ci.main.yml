name: CI (main) [deprecated]

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-24.04
    steps:
      - name: Legacy stub
        run: |
          echo "This workflow has been replaced by ci-lint-and-unit.yml and ci-golden-path.yml."
