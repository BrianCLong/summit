name: pm5-chatops

permissions:
  contents: read
on: issue_comment
jobs:
  forecast:
    if: contains(github.event.comment.body, '/forecast')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout @v4/**
      - run: node tools/pm/scenario.ts