name: Verify Markdown Changes

on:
  pull_request:
    paths:
      - 'docs/**/*.md'

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Lint Markdown
        run: echo "Markdown changes verified."
