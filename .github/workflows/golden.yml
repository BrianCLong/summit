name: Golden Lock
on: [pull_request]
jobs:
  check-golden:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Diff golden
        run: |
          if ! git diff --exit-code -- packages/testing/golden; then
            echo "::warning::Golden fixtures changed. Ensure contract versions bumped."
          fi
