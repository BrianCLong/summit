name: CI COS
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - run: pnpm test:cos
      - run: pnpm eval:cos
permissions:
  contents: read
