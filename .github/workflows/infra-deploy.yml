name: Infra Deploy
on:
  push:
    branches:
      - main
    paths:
      - 'src/platform/infra/**'

jobs:
  deploy:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Deploy Infra
        run: echo "Deploying infrastructure..."
