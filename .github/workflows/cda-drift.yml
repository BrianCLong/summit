name: CDA Drift Monitor
on:
  schedule:
    - cron: '0 0 * * 0' # Run weekly on Sunday

jobs:
  check-drift:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Run drift check
        run: python scripts/monitoring/pushback-over-validation-drift.py
