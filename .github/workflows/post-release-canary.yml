name: Post-Release Canary

on:
  schedule:
    - cron: "0 * * * *" # Run hourly
  workflow_dispatch:

jobs:
  canary:
    uses: ./.github/workflows/reusable/canary-rollback.yml
    with:
      service-name: summit
      rollback-enabled: false
