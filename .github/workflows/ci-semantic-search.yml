name: semantic-search-ci

on: [pull_request]

jobs:
  retrieval-tests:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - run: corepack enable && pnpm install
      - run: pnpm test
