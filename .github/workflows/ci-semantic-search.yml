name: semantic-search-ci

on: [pull_request]

jobs:
  retrieval-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - run: pnpm install
      - run: pnpm test
permissions:
  contents: read
