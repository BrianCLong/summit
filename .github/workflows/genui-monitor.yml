name: GenUI Monitor
on:
  schedule:
    - cron: '0 * * * *'
jobs:
  drift-check:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - name: Run Drift Script
        run: npx tsx scripts/monitoring/genui-drift.ts
