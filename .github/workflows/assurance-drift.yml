name: Assurance Posture Drift Monitor

on:
  schedule:
    - cron: '0 0 * * 1' # Weekly on Mondays
  workflow_dispatch:

jobs:
  drift-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Drift Detector
        run: ./scripts/monitoring/omb-m26-05-drift.sh
