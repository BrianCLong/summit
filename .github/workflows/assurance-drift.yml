name: Assurance Policy Drift Detection

on:
  schedule:
    - cron: '0 0 * * 0' # Weekly on Sunday
  workflow_dispatch:

jobs:
  drift-detection:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Drift Detector
        run: ./scripts/monitoring/omb-m26-05-drift.sh
