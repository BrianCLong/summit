name: benchmark-economy

on:
  pull_request:
    paths:
      - 'benchmarks/interactive/runners/economy_runner.*'
      - '.github/workflows/benchmark-economy.yml'
  push:
    branches: [main]

jobs:
  economy-benchmarks:
    uses: ./.github/workflows/_reusable-ci.yml
    with:
      run-tests: true
      workspace-filter: ""
