name: ci:shadowai-discovery

on:
  workflow_dispatch:
  pull_request:

jobs:
  shadowai-discovery:
    runs-on: ubuntu-latest
    steps:
      - name: CES shadow AI discovery gate (skeleton)
        run: echo "CES shadow AI discovery gate placeholder."
