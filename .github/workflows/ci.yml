name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  config-guard:
    name: config-guard
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node (from .nvmrc)
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - name: Enable Corepack
        run: corepack enable

      - name: Validate Jest & pnpm Configuration (pre-install)
        run: pnpm -w check:jest-config

  unit-tests:
    name: unit-tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [config-guard]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node (from .nvmrc)
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: "pnpm"

      - name: Enable Corepack
        run: corepack enable

      - name: Install (frozen lockfile)
        run: pnpm -w install --frozen-lockfile

      - name: Unit Tests
        run: pnpm -w test:unit

  governance-cas-integrity:
    name: Governance / CAS Integrity
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [unit-tests]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node (from .nvmrc)
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: "pnpm"

      - name: Enable Corepack
        run: corepack enable

      - name: Install (frozen lockfile)
        run: pnpm -w install --frozen-lockfile

      - name: Generate evidence run (SBOM + provenance)
        run: node scripts/ci/generate_evidence_run.mjs

      - name: GA verify
        run: pnpm ga:verify

      - name: Verify CAS (evidence)
        run: pnpm ci:cas:verify -- --mode all --run artifacts/evidence/${{ github.sha }}

      - name: Verify CAS (ga-verify)
        run: pnpm ci:cas:verify -- --mode all --run artifacts/ga-verify/${{ github.sha }}

      - name: Upload CAS verification reports
        uses: actions/upload-artifact@v4
        with:
          name: cas-verify-${{ github.sha }}
          path: artifacts/governance/cas-verify/**
