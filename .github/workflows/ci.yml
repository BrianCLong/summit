name: CI (legacy)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy stub
        run: |
          echo "The previous multi-lane CI workflow has been retired."
          echo "Use ci-lint-and-unit.yml for lint/tests and ci-golden-path.yml for the smoke path."
