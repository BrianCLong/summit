name: etl-canary

permissions:
  contents: read
on: [pull_request]
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python pipelines/airflow/plugins/data_diff.py