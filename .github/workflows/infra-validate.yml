name: Infra Validate
on:
  push:
    paths:
      - 'src/platform/infra/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Validate Infra
        run: echo "Validating infrastructure modules..."
permissions:
  contents: read
