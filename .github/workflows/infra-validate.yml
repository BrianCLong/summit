name: Infra Validate
on:
  push:
    paths:
      - 'src/platform/infra/**'

jobs:
  validate:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Validate Infra
        run: echo "Validating infrastructure modules..."
