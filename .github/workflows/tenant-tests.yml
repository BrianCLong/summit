name: tenant-tests

permissions:
  contents: read
on: [pull_request]
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pnpm install --frozen-lockfile && pnpm test tests/tenant/isolation.test.ts