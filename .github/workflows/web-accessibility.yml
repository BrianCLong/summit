name: web-accessibility

on:
  pull_request:
    paths:
      - "apps/web/**"
      - ".github/workflows/web-accessibility.yml"
      - "docs/ui/ACCESSIBILITY.md"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  lint-and-smoke-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332 # v4

      - uses: actions/setup-node@1d1121627393d3093b167069a74422b94a9ec3f2 # v4
        with:
          node-version: 20
          cache: "pnpm"
          cache-dependency-path: apps/web/pnpm-lock.yaml

      - uses: pnpm/action-setup@feaa6391d8481eb0db9d5b066fba94be3691684c # v4
        # version is read from package.json packageManager field

      - name: Install dependencies
        working-directory: apps/web
        run: pnpm install --frozen-lockfile

      - name: Accessibility lint
        working-directory: apps/web
        run: pnpm run lint:a11y

      - name: Accessibility smoke test
        working-directory: apps/web
        run: pnpm run test -- --run src/__tests__/accessibility/landmarkKeyboardFlow.test.tsx
