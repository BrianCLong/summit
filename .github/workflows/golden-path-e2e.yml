name: Golden Path E2E

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 2 * * *" # Nightly at 2 AM
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
<<<<<<< HEAD
  golden-path-investigations:
    name: Golden Path - Investigations
    if: false  # Temporarily disabled for GA merge surge
=======
  golden-path-e2e:
>>>>>>> d58f336637 (feat(ndd): Init Narrative Dynamics Module & Fix CI Evidence Check)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "pnpm"

      - name: Install Dependencies
        run: pnpm install

      - name: Install Playwright Browsers
        run: |
          cd e2e/golden-path
          pnpm exec playwright install --with-deps chromium

      - name: Run Golden Path E2E
        env:
          BASE_URL: http://localhost:3000
          GOLDEN_PATH_E2E_ENABLED: ${{ (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') && '1' || '0' }}
          GOLDEN_PATH_JOURNEY: ${{ (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') && 'full' || 'basic' }}
        run: ./scripts/ci/run_golden_path_e2e.sh

      - name: Upload Playwright Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report
          retention-days: 7

      - name: Upload Playwright Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
<<<<<<< HEAD
          name: test-results-investigations
          path: test-results
          retention-days: 7

  golden-path-frontend:
    name: Golden Path - Consolidated Frontend
    if: false  # Temporarily disabled for GA merge surge
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: 'pnpm'

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright Browsers
        run: pnpm exec playwright install --with-deps chromium

      - name: Run Frontend Golden Path
        env:
          BASE_URL: http://localhost:3000
          PLAYWRIGHT_USE_WEBSERVER: 'true'
        run: pnpm test:e2e:golden-path

      - name: Upload Playwright Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-frontend
          path: reports/playwright
          retention-days: 7

      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-frontend
=======
          name: playwright-artifacts
>>>>>>> d58f336637 (feat(ndd): Init Narrative Dynamics Module & Fix CI Evidence Check)
          path: test-results
          retention-days: 7

      - name: Upload Golden Path Evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: golden-path-evidence
          path: artifacts/evidence/goldenpath-frontend/
          retention-days: 7

      - name: Upload Summit Evidence Bundle
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: summit-evidence
          path: artifacts/evidence/
          retention-days: 7
