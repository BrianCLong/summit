name: Golden Path E2E

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
<<<<<<< HEAD
    - cron: '0 2 * * *' # Nightly at 2 AM
  workflow_dispatch:

jobs:
  e2e:
=======
    - cron: '0 0 * * *' # Nightly
  workflow_dispatch:

jobs:
  golden-path-e2e:
>>>>>>> 50f8d7925a (feat: add golden path E2E test harness for consolidated frontend)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

<<<<<<< HEAD
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install E2E dependencies
        run: |
          cd e2e/golden-path
          npm ci
          npx playwright install --with-deps

      - name: Run Golden Path E2E
        run: ./scripts/ci/run_golden_path_e2e.sh
        env:
          GOLDEN_PATH_E2E_ENABLED: ${{ (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') && '1' || '0' }}
          GOLDEN_PATH_JOURNEY: ${{ (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') && 'full' || 'basic' }}
          BASE_URL: http://localhost:3000
=======
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'pnpm'

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Install Dependencies
        run: pnpm install

      - name: Install Playwright Browsers
        run: |
          cd e2e/golden-path
          npx playwright install --with-deps chromium

      - name: Run Golden Path E2E
        env:
          GOLDEN_PATH_E2E_ENABLED: ${{ github.event_name == 'schedule' && '1' || '0' }}
          # On PRs, default to 0 unless manually triggered or labeled (logic simplified here)
          # Actually, plan said "PR trigger with GOLDEN_PATH_E2E_ENABLED=0 initially (safe), nightly with =1"
          # But for verification purpose, I should probably enable it or allow manual trigger to enable.
          # I'll set it to 1 on nightly, 0 on PR/Push by default.
          # To test on PR, user can change this line or use workflow_dispatch.
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
             export GOLDEN_PATH_E2E_ENABLED=1
          fi
          scripts/ci/run_golden_path_e2e.sh

      - name: Upload Playwright Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: artifacts/e2e/playwright-report/
          retention-days: 7
>>>>>>> 50f8d7925a (feat: add golden path E2E test harness for consolidated frontend)

      - name: Upload Evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
<<<<<<< HEAD
          name: golden-path-evidence
          path: artifacts/evidence/goldenpath-frontend/
          retention-days: 7

      - name: Upload Playwright Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: artifacts/e2e/playwright-output/
=======
          name: summit-evidence
          path: artifacts/evidence/
>>>>>>> 50f8d7925a (feat: add golden path E2E test harness for consolidated frontend)
          retention-days: 7
