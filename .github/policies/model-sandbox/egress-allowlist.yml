# .github/policies/model-sandbox/egress-allowlist.yml
version: 1
egress:
  mode: deny-all # Options: deny-all, allowlist
  allowlist:
    # - host: "huggingface.co"
    #   port: 443
    # - host: "github.com"
    #   port: 443
