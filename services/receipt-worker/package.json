{
  "name": "@intelgraph/receipt-worker",
  "version": "4.2.3",
  "description": "Background worker for processing provenance receipts with DLQ and metrics.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm run build && jest --config jest.config.cjs"
  },
  "dependencies": {
    "@intelgraph/provenance": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.17.5",
    "jest": "^29.7.0",
    "typescript": "^5.7.3"
  }
}
