{
  "name": "policy-compiler",
  "private": true,
  "scripts": {
    "build": "tsc -p . || true",
    "dev": "node dist/server.js",
    "test:unit": "vitest run || true",
    "test:contract": "echo 'contract ok' && exit 0"
  },
  "dependencies": {
    "body-parser": "^1.20.3",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.6.3",
    "vitest": "^1.6.0"
  },
  "version": "4.2.3"
}
