apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "docling-svc.fullname" . }}-config
  labels:
    {{- include "docling-svc.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
