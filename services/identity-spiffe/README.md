# Identity SPIFFE Service

Prototype service issuing SPIFFE IDs (SVIDs) for IntelGraph workloads. The
controller exposes minimal endpoints and will integrate with a SPIRE server in a
future iteration.
