# GQL Compatibility

To check GQL compatibility against Cypher and view the supported subset, run `make gql-compat`.

This command will output the compatibility matrix at `artifacts/gql-support-matrix.json`.
