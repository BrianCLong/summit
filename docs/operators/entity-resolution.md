# Entity Resolution Operators Guide

ER policies are located in `policy/entity-resolution/v<version>`.

To test diffing policy versions and view deterministic merges:
Run `make er`. This generates the `artifacts/entity-resolution/er_policy_report.json` to show rule matches and rationale.
