# Dependency Delta: LLM Selection Framework

## PR Context
- **PR**: feat/llm-selection-framework
- **Date**: 2026-02-04

## Changes
- No new external dependencies added in Lane 1.
- `summit/evidence` uses standard library (`json`, `pathlib`).
- `summit/eval_harness` uses standard library.
- `summit/router` uses standard library.

## Verification
- Verified against `pyproject.toml` and `package.json` (no changes to deps).
