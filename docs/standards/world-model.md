# World Model Standards

## Evidence IDs

- EVID:<source>:<id> format is strictly enforced.

## Actions

- Must pass `evaluateMutation` checks.
- Deny-by-default is enabled for all mutating state changes.
