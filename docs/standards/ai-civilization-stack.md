# AI Civilization Stack Standards
Deterministic seed required in every run.
Evidence ID format: EID:CIV:<module>:<scenario>:<nnnn>
No unstable timestamps in report.json or metrics.json; only stamp.json may contain wall-clock time.
