# TODO

- Implement WebAuthn step-up authentication.
- Add performance optimizations to the `findDuplicateCandidates` method in `SimilarityService.js`.
- Add notifications to the user in the `DeduplicationInspector` component when a merge fails.
- Add a loading state to the `DeduplicationInspector` component when a merge is in progress.
- Add a way for the user to see more details about the entities being compared in the `DeduplicationInspector` component.
- Add a way for the user to adjust the similarity threshold in the `DeduplicationInspector` component.
