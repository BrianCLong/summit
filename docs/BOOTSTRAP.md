# Summit Bootstrapping Guide

## 1. Install Dependencies
Ensure you have the required runtime (Node/Python) and tools (git, make).

## 2. Initialize Summit
Run the initialization script to set up the environment:
```bash
./bootstrap/cli/summit-dev.sh
```

## 3. Configuration
Review `bootstrap/summit.yaml` and adjust settings if necessary.
