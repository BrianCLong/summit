# Developer Experience, Tooling, and Workflow Review: 2026-01-14

## DX Ground Truth Snapshot

*   **Setup Path Status:** 🟡 Yellow
*   **Top Friction Items:**
    1.  `scripts/bootstrap.sh` takes >5 mins on fresh clone.
    2.  `verify:ga-security.ts` is too slow for frequent local execution.
    3.  `test:smoke` is flaky on local machines (timing issues).
*   **CI-to-Local Mismatches:**
    *   CI uses optimized Docker layers; local `bootstrap.sh` rebuilds more than necessary.

## Objectives (max 3)

1.  **Reduce 'fresh clone to tests passing' time by 50%**
    *   *Acceptance Criteria:* `scripts/bootstrap.sh` completes in < 2 minutes using aggressive caching.
2.  **Eliminate top setup failure mode (Security Verify Slowness)**
    *   *Acceptance Criteria:* Implement `verify:ga-security:lite` that runs in <30s for pre-commit hooks.
3.  **Stabilize `test:smoke`**
    *   *Acceptance Criteria:* Test suite passes 50/50 runs on standard dev machine.

## Execution Plan (P0–P3)

| Task ID | Priority | Owner | Friction Item | Description | Verification |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **DX-20260114-01** | **P1** | Codex CLI | Bootstrap Slowness | Optimize `scripts/bootstrap.sh` to use pnpm store and build caching. | Measure execution time. |
| **DX-20260114-02** | **P2** | Codex CLI | Security Verify | Create `verify:ga-security:lite` skipping heavy scans. | Run script, check time & coverage. |
| **DX-20260114-03** | **P0** | Codex CLI | Flaky Tests | Fix race conditions in `test:smoke` (wait for services). | Run loop test. |

## Agent Dispatch

| Agent | Work Order |
| :--- | :--- |
| **Codex CLI** | Implement script optimizations (bootstrap, verify) and fix tests. |
| **Atlas** | Integrate `verify:ga-security:lite` into pre-commit config. |
| **Antigravity** | Ensure lite verification doesn't compromise GA gates. |

## Cross-System Actions

*   [ ] **GitHub Projects:** Create/update DX cards with repro and acceptance criteria; link review doc.
*   [ ] **Linear:** Create/update issues for P0/P1 tooling changes; link doc.
*   [ ] **Jira:** Create/update tooling tasks; link doc.
*   [ ] **Notion:** Update onboarding/troubleshooting with verified steps; link review doc.
