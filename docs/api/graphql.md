# GraphQL API

The GraphQL API is the primary interface for fetching structured data from the Summit platform.
You can explore the API using the GraphQL Playground available at `/api/graphql`.

*Note: Additional mutation capabilities, such as advanced data ingestion operations, will be added in upcoming releases.*
