# Competitive Intelligence Evidence

Evidence envelopes live under `docs/ci/evidence/<target_slug>/` and are append-only. Each claim
must map to a public source and appear in `CLAIMS.yml`.

Use the templates in `docs/ci/competitive-intel/templates/` to initialize new targets.

