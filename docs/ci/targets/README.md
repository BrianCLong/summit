# Competitive Intelligence Targets

Each target entry is stored as `docs/ci/targets/<target_slug>.yml` and must align to the
Summit Readiness Assertion and Meta-Governance authority chain. Use the template in
`docs/ci/competitive-intel/templates/target.template.yml`.

