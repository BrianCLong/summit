*** a/conductor-ui/frontend/src/maestro/components/ReplayDialog.tsx
--- b/conductor-ui/frontend/src/maestro/components/ReplayDialog.tsx
@@
-import React from 'react';
+import React, { useEffect, useRef } from 'react';
 import { Button } from '@/components/ui/button';
+import useFocusTrap from '../utils/useFocusTrap';
@@
-export default function ReplayDialog({ open, onClose, ...props }) {
-  if (!open) return null;
-  return (
-    <div className="fixed inset-0 bg-black/40 flex items-center justify-center">
-      <div className="bg-white rounded-2xl p-4 w-[520px]">
-        <h2 className="text-lg font-medium">Replay run</h2>
-        {/* form fields… */}
-        <div className="mt-4 flex justify-end gap-2">
-          <Button onClick={onClose} variant="outline">Cancel</Button>
-          <Button>Replay</Button>
-        </div>
-      </div>
-    </div>
-  );
-}
+export default function ReplayDialog({ open, onClose, ...props }) {
+  const containerRef = useRef<HTMLDivElement | null>(null);
+  useFocusTrap(containerRef, open);
+  useEffect(() => {
+    if (open && containerRef.current) {
+      const h = containerRef.current.querySelector('h2');
+      (h as HTMLElement | null)?.focus?.();
+    }
+  }, [open]);
+
+  if (!open) return null;
+  return (
+    <div className="fixed inset-0 bg-black/40 flex items-center justify-center" role="dialog" aria-modal="true" aria-labelledby="replay-title" aria-describedby="replay-desc">
+      <div ref={containerRef} className="bg-white rounded-2xl p-4 w-[520px] outline-none">
+        <h2 id="replay-title" tabIndex={-1} className="text-lg font-medium">Replay run</h2>
+        <p id="replay-desc" className="text-sm text-gray-600">Re-executes the run with the same inputs. You can override select parameters below.</p>
+        {/* form fields… */}
+        <div className="mt-4 flex justify-end gap-2">
+          <Button onClick={onClose} variant="outline">Cancel</Button>
+          <Button>Replay</Button>
+        </div>
+      </div>
+    </div>
+  );
+}
