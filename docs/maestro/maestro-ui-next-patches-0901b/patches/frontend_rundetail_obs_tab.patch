*** a/conductor-ui/frontend/src/maestro/pages/RunDetail.tsx
--- b/conductor-ui/frontend/src/maestro/pages/RunDetail.tsx
@@
-  const tabs = ['Graph','Logs','Artifacts','CI','Timeline'];
+  const tabs = ['Graph','Logs','Artifacts','CI','Timeline','Obs'];
@@
-  {activeTab === 'CI' && <CITab runId={id} />}
+  {activeTab === 'CI' && <CITab runId={id} />}
+  {activeTab === 'Obs' && (
+    <div className="space-y-3">
+      <p className="text-sm text-gray-600">Quick Observability embeds (configurable via window.__MAESTRO_CFG__)</p>
+      <GrafanaPanel uid={(window as any).__MAESTRO_CFG__?.grafanaDashboards?.overview || 'maestro-overview'} title="Overview" />
+    </div>
+  )}
