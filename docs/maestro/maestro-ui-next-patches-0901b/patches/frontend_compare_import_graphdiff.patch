*** a/conductor-ui/frontend/src/maestro/pages/RunCompare.tsx
--- b/conductor-ui/frontend/src/maestro/pages/RunCompare.tsx
@@
+import GraphDiff from '../components/GraphDiff';
@@
 export default function RunComparePage() {
@@
-  return (
+  return (
     <div className="p-6 space-y-4">
@@
-      <section className="grid grid-cols-1 md:grid-cols-2 gap-4">
+      <section className="grid grid-cols-1 md:grid-cols-2 gap-4">
         <div className="border rounded-xl p-4">
           <h2 className="font-medium">Totals</h2>
@@
         </div>
       </section>
+
+      {/* Graph diff (if API already provides node timings/costs) */}
+      {Array.isArray((data as any).currentGraph?.nodes) && Array.isArray((data as any).previousGraph?.nodes) && (
+        <GraphDiff current={(data as any).currentGraph} previous={(data as any).previousGraph} nodeDeltas={Object.fromEntries((data.nodes || []).map(n => [n.id, n]))} />
+      )}
     </div>
   );
 }
