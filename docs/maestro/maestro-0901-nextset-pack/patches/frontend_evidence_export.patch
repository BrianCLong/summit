*** a/conductor-ui/frontend/src/maestro/pages/RunDetail.tsx
--- b/conductor-ui/frontend/src/maestro/pages/RunDetail.tsx
@@
-import React, { useState, useEffect } from 'react';
+import React, { useState, useEffect } from 'react';
 import ScorecardsTab from './ScorecardsTab';
 import HITLBar from '../components/HITLBar';
 import RouteDecisionPanel from '../components/RouteDecisionPanel';
+import { downloadJSON } from '../utils/download';
+import { getRunEvidence } from '../api';
@@
-  const tabs = ['Graph','Logs','Artifacts','CI','Timeline','Scorecards'];
+  const tabs = ['Graph','Logs','Artifacts','CI','Timeline','Scorecards','Evidence'];
+  const [evidence, setEvidence] = useState<any|null>(null);
+  useEffect(() => {
+    if (activeTab === 'Evidence') {
+      getRunEvidence?.(id).then(setEvidence).catch(()=>{});
+    }
+  }, [activeTab, id]);
@@
   {activeTab === 'Scorecards' && <ScorecardsTab runId={id} />}
+  {activeTab === 'Evidence' && (
+    <section className="mt-3 border rounded-xl p-3 bg-white shadow-sm space-y-3" aria-label="Evidence">
+      <div className="flex items-center justify-between">
+        <h2 className="text-lg font-medium">Provenance & Attestations</h2>
+        <button className="border rounded px-3 py-1" onClick={()=>evidence && downloadJSON(`run_${id}_provenance.json`, evidence)}>Export JSON</button>
+      </div>
+      {evidence ? (
+        <div className="space-y-2 text-sm">
+          <div className="flex gap-3">
+            <span className={"px-2 py-0.5 rounded border " + (evidence.cosignVerified ? "bg-green-50" : "bg-yellow-50")}>Cosign: {evidence.cosignVerified ? "verified" : "unverified"}</span>
+            <span className="px-2 py-0.5 rounded border">SLSA: {evidence.slsaLevel || 'n/a'}</span>
+            <span className="px-2 py-0.5 rounded border">SBOM: {evidence.sbomUrl ? <a className="underline" href={evidence.sbomUrl} target="_blank" rel="noreferrer">open</a> : 'not provided'}</span>
+          </div>
+          <table className="w-full text-sm border rounded">
+            <thead className="bg-gray-50">
+              <tr><th className="text-left px-2 py-1">Attestation</th><th className="text-left px-2 py-1">Issuer</th><th className="text-left px-2 py-1">Summary</th></tr>
+            </thead>
+            <tbody>
+              {(evidence.attestations||[]).map((a:any,i:number)=>(
+                <tr key={i} className="border-t"><td className="px-2 py-1">{a.name}</td><td className="px-2 py-1">{a.issuer}</td><td className="px-2 py-1">{a.summary}</td></tr>
+              ))}
+            </tbody>
+          </table>
+        </div>
+      ) : <div className="text-sm text-gray-600">Loading evidence…</div>}
+    </section>
+  )}
