# Modulith Data Handling

- Do not log source file contents.
- Limit logs/artifacts to module names, relative file paths, and violation metadata.
- Never persist secrets from scanned files.
- Retention target for modulith artifacts is controlled by CI retention policy.
