# Weekly Release Digest (Last 7 Days)

## Executive Summary
* Overall readiness: **Yellow**
* CI instability has increased slightly, particularly around flaky end-to-end tests and intermittent database startup failures.
* A massive surge in stabilization and compliance PRs successfully merged.
* Governance alignment is generally sound, though some specific evidence artifacts required hotfixes.
* Biggest risk: Nondeterministic CI failures breaking the release pipeline flow.
* Highest-leverage next action: Address resource starvation/timeout issues in the Jest integration test suite.

---

## CI Health Snapshot
* Pass rate trend: ~85% (Downward slightly due to recent surge of merges)
* Duration trend: Increasing (Some jobs hitting 30m timeouts)
* Flake estimate: ~10-15% of failures (mostly integration tests & playwright timeouts)
* Required checks status: **Failing intermittently**
* Top 3 unstable workflows + hypotheses:
  1. `build-and-verify` (Integration tests): Failing due to Jest timeout (30000ms exceeded). Hypothesis: Connection leaks, resource starvation, or missing `afterAll` teardowns in `server/src/maestro/__tests__/integration.test.ts`.
  2. `gate/evidence_contract`: Fails due to JSON schema validation errors in newly generated evidence artifacts. Hypothesis: Drift between Python evidence scripts and expected `report.json` schemas.
  3. `verify-gateway-build`: Docker buildx failure accessing syft scanner image. Hypothesis: Intermittent DNS/network failure in the Github Actions runner reaching `registry-1.docker.io`.

**If we did only 3 things to harden CI next week:**
1. Fix test leakage and timeouts in `server/src/maestro/__tests__/integration.test.ts`.
2. Standardize Python evidence payload schema validation.
3. Cache the `buildkit-syft-scanner` image to avoid network-related build failures.

---

## Merges & Delivery
* **Features**
  * PR #18810 - Added Release Digest Automation
* **Fixes**
  * PR #18835 - fix/remove-final-corrupted-file
* **Infra/CI**
  * PR #18820 - Update pnpm cache keys
* **Governance/Evidence**
  * PR #18840 - Add missing header to CONTROL_REGISTRY.md

**Notable / Risky Changes**
* The high volume of recent merges poses a risk of subtle regressions. Database schema changes require close monitoring.

---

## Governance & Evidence
**Status: Drift Detected**

* Branch protection alignment: Aligned
* Required check drift: Minor discrepancies resolved
* Evidence completeness summary: Several evidence schemas failed validation in `gate/evidence_contract`.
* Top gaps (Blocking / Latent / Hygiene):
  * **Blocking:** Invalid JSON payload formats in `EVD-REPLITAGENT-REPAIR-001` and `EVD-IOB20260202-ECONESP-001`.
  * **Hygiene:** `CHANGELOG.md` validation occasionally failing when purely documentation PRs omit it.

---

## Incidents & Anomalies
Date | Event | Impact | Duration | Status | Follow-up
---|---|---|---|---|---
2026-03-01 | Docker Hub DNS failure | `verify-gateway-build` blocked | Intermittent | Resolved (Runner reset) | Implement registry caching/mirrors.
2026-03-01 | Jest Integration Timeout | CI blockage on `main` | Ongoing | Active | Debug `integration.test.ts` async handles.

---

## Risks & Blockers
1. **Integration Test Deadlocks**
   * Severity: High
   * Likelihood: High
   * Mitigation: Implement strict `--detectOpenHandles` and increase Jest timeouts temporarily.
   * Owner role: Platform Team

2. **Evidence Schema Desync**
   * Severity: Medium
   * Likelihood: Medium
   * Mitigation: Establish strict schema definitions for `report.json` and prevent unverified scripts from pushing to `evidence/`.
   * Owner role: Compliance Team

---

## Next-Week Priorities (Ranked)
1. **Fix Integration Tests:** Resolve the 30000ms timeouts in `maestro` tests.
   * Acceptance criteria: `pnpm test:integration` passes locally and in CI.
   * Expected impact: Restores golden path pipeline stability.
2. **Resolve Evidence Schema Failures:** Fix the python generation scripts for `report.json`.
   * Acceptance criteria: `ci/gates/evidence_contract.sh` passes 100%.
   * Expected impact: Unblocks GA compliance evidence gating.
