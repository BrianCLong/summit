# PR 01: Schemas: InvestigationRun + EvidenceObject

## Summary
Adds schemas for the deterministic artifacts InvestigationRun, EvidenceObject, and EvidenceBundle according to the Subsumption Program specifications.
Also adds a gate script `scripts/gates/validate_schemas.mjs` and required fixtures.

## Gates Affected
- `gate:schema:validate` (`scripts/gates/validate_schemas.mjs`)

## Local Run Instructions
Run `node scripts/gates/validate_schemas.mjs` to execute the tests.

## Fixture Notes
Added minimum valid and invalid fixtures to verify the basic schema rules for the three target schemas.
