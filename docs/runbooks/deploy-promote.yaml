apiVersion: maestro/v1
kind: Runbook
metadata:
  name: deploy-promote
  version: 0.6.0
  owner: platform@summit
  approvals:
    required: true
    approvers: ["release-manager"]
spec:
  inputs:
    - name: service
      type: string
      required: true
    - name: from
      type: string
      required: true
    - name: to
      type: string
      required: true
  workflowRef: deploy-promote@0.6.0
