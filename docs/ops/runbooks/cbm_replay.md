# CBM Replay Operator Controls

Replay mode is meant to be run in CI to enforce determinism constraints and performance budgets.
Outputs should be byte-for-byte identical given the same config and event logs.
