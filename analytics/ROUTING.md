# Analytics Routing

- `predictive` agent uses `risk_hotspots` and `tech_debt_forecast` to:
  - prioritize improvement tasks
  - recommend modules for Jules to refactor
- `auto_improve_daily` flow reads deficits → passes to Predictive.
