{
  "name": "prov-ledger",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "exports": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && vitest run tests/provenance-ledger.test.ts && node --test tests/*.js"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.0",
    "vitest": "^1.5.0"
  }
}
