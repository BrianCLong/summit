[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pckd"
version = "0.1.0"
description = "Policy-constrained knowledge distillation utilities with compliance attestations."
readme = "README.md"
authors = [{name = "Summit AI"}]
license = {text = "Apache-2.0"}
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.24,<2.0"
]

[project.urls]
Homepage = "https://example.com/pckd"
