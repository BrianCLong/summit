{
  "name": "ga-graphai-root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run -ws --if-present test",
    "smoke": "npm run -ws --if-present test:smoke",
    "load": "npm run -ws --if-present test:load"
  }
}
