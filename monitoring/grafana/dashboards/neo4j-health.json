{
  "title": "Neo4j Health",
  "panels": [
    {
      "type": "gauge",
      "title": "Neo4j Connectivity",
      "targets": [{ "expr": "neo4j_connectivity_up" }]
    },
    {
      "type": "graph",
      "title": "Neo4j Query p95 Latency",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(neo4j_query_latency_ms_bucket[5m])) by (le))"
        }
      ]
    }
  ],
  "schemaVersion": 16,
  "version": 0
}
