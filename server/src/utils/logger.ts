import logger, {
  getLogContext,
  LogContext,
  setLogContext,
  withLogContext,
} from '../config/logger';

export { logger, getLogContext, setLogContext, withLogContext, LogContext };
export default logger;
