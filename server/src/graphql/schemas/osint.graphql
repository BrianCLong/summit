
type OsintScan {
  id: ID!
  target: String!
  status: String!
}

type Query {
  osintScans: [OsintScan]
  osintScan(id: ID!): OsintScan
}

type Mutation {
  startOsintScan(target: String!): OsintScan
}
