def detect_coordination_anomalies(graph_features: dict) -> list[dict]:
    """
    Defensive-only stub.
    Input: graph-derived coordination signals (no raw content).
    Output: list[IWAlert] dicts (must validate schemas/cogwar/iw_alert.schema.json)
    """
    return []
