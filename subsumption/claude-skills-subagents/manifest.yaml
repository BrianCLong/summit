version: 1
item:
  title: "Claude Skills and Subagents: Escaping the Prompt Engineering Hamster Wheel"
  links: []
  type: "article"
  date_version: "2024-03-24" # Putting a placeholder date
  intriguing_claim: "Static prompt engineering is not a scalable architecture for production AI systems; instead, agentic systems with modular execution (Skills) and separation of concerns (Subagents) are required."
item_slug: "claude-skills-subagents"

claim_registry:
  mode: "missing-item"
  claims_file: "subsumption/claude-skills-subagents/claims.md"

prs:
  - title: "feat(architecture): implement skills and subagents orchestration"
    lane: "foundation"
    owner: "Codex"
    risk: "green"

gates:
  - name: "verify-skills-subagents-architecture"
    required_checks_status: "assumed"
    deny_by_default_required: true

evidence:
  schemas:
    report: "evidence/schemas/report.schema.json"
    metrics: "evidence/schemas/metrics.schema.json"
    stamp: "evidence/schemas/stamp.schema.json"
  index: "evidence/index.json"
  required_ids:
    - "EVD-SKILLS-SUBAGENTS-ARCH-001"

docs_targets:
  - "docs/architecture/skills-subagents.md"
  - "docs/decisions/skills-subagents.md"
  - "docs/ops/runbooks/skills-subagents.md"

fixtures:
  deny_policy_dir: "subsumption/claude-skills-subagents/fixtures/policy/deny"
  tests:
    positive:
      - "subsumption/claude-skills-subagents/fixtures/tests/positive/minimal_bundle"
    negative:
      - "subsumption/claude-skills-subagents/fixtures/tests/negative/missing_docs"

feature_flags: []
dependency_delta:
  required_if_deps_change: true
  doc_root: "deps_delta"

required_checks_discovery:
  status: "assumed"
  instructions: "docs/required_checks.todo.md"
