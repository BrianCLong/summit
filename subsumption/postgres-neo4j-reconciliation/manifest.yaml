name: "Deterministic Postgres to Neo4j Reconciliation"
type: "spec/pattern"
version: "1.0.0"
description: "A battle-tested spec for deterministically syncing Postgres to Neo4j using per-row digests, WAL/CDC LSN windows, and explicit tombstone replays."
status: "proposed"
tags:
  - postgres
  - neo4j
  - debezium
  - reconciliation
  - idempotent
