# Allow Fixtures (Ingress NGINX Retirement)

This directory documents allowlist fixtures for the ingress-nginx retirement bundle.
Use `tests/fixtures/allow_gateway_api` for a Gateway API-positive case.
