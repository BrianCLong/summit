# Deny Fixtures (Ingress NGINX Retirement)

This directory documents deny-by-default fixtures for the ingress-nginx retirement bundle.
Use `tests/fixtures/deny_ingress_nginx` and `tests/fixtures/deny_ingress_annotations` for
concrete negative cases.
