# Deny Policy Fixtures

This directory contains `deny-by-default` fixtures used to ensure the Behavior Forecasting Platform correctly triggers a deny status on unapproved AI trajectories or policy violations.
