version: 1
item:
  title: "AgentVista: Evaluating Multimodal Agents in Ultra-Challenging Realistic Visual Scenarios"
  links: ["https://arxiv.org/abs/2602.23166"]
  type: "academic_paper"
  date_version: "2026-02-26"
  intriguing_claim: "AgentVista exposes that current multimodal agents (including Gemini-3-Pro) fail severely at long-horizon hybrid tool use, exposing a major execution gap."
item_slug: "agentvista"

claim_registry:
  mode: "active"
  claims_file: "subsumption/agentvista/claims.md"

prs:
  - title: "feat(benchmark): add agentvista benchmark scaffold"
    lane: "evaluation"
    owner: "master"
    risk: "green"

gates:
  - name: "verify-agentvista-bundle"
    required_checks_status: "assumed"
    deny_by_default_required: true

evidence:
  schemas:
    report: "evidence/schemas/report.schema.json"
    metrics: "evidence/schemas/metrics.schema.json"
    stamp: "evidence/schemas/stamp.schema.json"
  index: "evidence/index.json"
  required_ids:
    - "EVD-SUBSUMPTION-AGENTVISTA-001"

docs_targets:
  - "docs/standards/agentvista.md"
  - "docs/security/data-handling/agentvista.md"
  - "docs/ops/runbooks/agentvista.md"
  - "docs/repo_assumptions.md"

fixtures:
  deny_policy_dir: "subsumption/agentvista/fixtures/policy/deny"
  tests:
    positive:
      - "subsumption/agentvista/fixtures/tests/positive/minimal_bundle"
    negative:
      - "subsumption/agentvista/fixtures/tests/negative/missing_docs"

feature_flags: []
dependency_delta:
  required_if_deps_change: true
  doc_root: "deps_delta"

required_checks_discovery:
  status: "assumed"
  instructions: "docs/required_checks.todo.md"
