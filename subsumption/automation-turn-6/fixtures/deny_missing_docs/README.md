# Deny Missing Docs Fixture
This fixture is used to test that the verifier correctly fails when required documentation is missing (simulated by pointing the verifier here and seeing failure, though the self-test currently just checks existence of this README).
