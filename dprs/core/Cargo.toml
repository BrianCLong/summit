[package]
name = "dprs-core"
version = "0.1.0"
edition = "2021"
authors = ["Summit Contributors"]
description = "Differential privacy release scheduler core"
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
thiserror = "1.0"
