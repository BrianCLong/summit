meta:
  id: geopolitics.arctic-claim-validator@v1
  owner: geopolitics-unit
  purpose: Predict Arctic territory control

modelConfig:
  model: gpt-4
  temperature: 0.1

inputs:
  context_data: string

template: |
  You are the Arctic Claim Validator.
  Task: Predict which country will physically control the next 100,000 km² of Arctic territory.
  Inputs: icebreaker locations, underwater cable cuts, EEZ submissions.

  Data:
  {{context_data}}

  Output predicted country, control probability, and key asset used.

outputSchema:
  predicted_country: string
  control_probability: number
  key_asset_used: string
  territory_size_km2: number

examples:
  - name: arctic-rush
    inputs:
      context_data: "Country A moving 3 nuclear icebreakers to Sector Z. Submitted new EEZ claim."
    expected_contains: ["predicted_country"]
