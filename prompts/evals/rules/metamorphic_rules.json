{
  "rules_version": "1.0.0",
  "transforms": [
    {
      "id": "mm.reorder_participants",
      "description": "Reorder participants array (reverse) to verify stability of structure under input ordering.",
      "erq": "reverse_array",
      "path": "participants"
    },
    {
      "id": "mm.add_irrelevant_context",
      "description": "Add irrelevant context_summary suffix; should not break schema or invariants.",
      "erq": "append_string",
      "path": "context_summary",
      "value": " Irrelevant: The sky is blue; this should not affect required fields."
    }
  ]
}
