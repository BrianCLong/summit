Scaffold Summit silent-intent foundations: evidence bundle entry, biometric gate policy +
tests, input provider/router interfaces, and dependency delta guard scripts. Update the
roadmap status accordingly. Keep changes minimal, deny-by-default for biometric inference,
and add tests covering gate behavior and never-log enforcement.
