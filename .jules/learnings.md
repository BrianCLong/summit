## YYYY-MM-DD - Trust Control Plane
**Learning:** When modifying `evidence/index.json` or JSON schema files, make sure to add to the existing structure rather than overwriting it, as it will break the existing validation pipeline.
**Action:** When working with evidence/schema files, carefully read and modify the existing JSON structures.
