apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: db-credentials-dev
  namespace: default
spec:
  encryptedData:
    # created via: echo -n "secret" | kubeseal ...
    password: AgBy3i4f...
  template:
    metadata:
      name: db-credentials-dev
    type: Opaque
    data:
      password: "PLACEHOLDER_FOR_DECRYPTED"
