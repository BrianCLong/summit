# Accessibility Keyboard Gate Reports

This directory stores the accessibility keyboard gate artifacts produced by CI.

- Use `pnpm run test:a11y-gate` to regenerate.
- Evidence artifacts are expected to be deterministic between runs.
