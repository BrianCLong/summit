# Optional Agent Runner (Lane 2)

This directory contains the skeleton for an optional background agent runner.

## Status
**EXPERIMENTAL**: This module is feature-flagged OFF by default.

## Features
- Background execution of PromptPack tasks.
- Integration with MCP allowlisted servers.
- Automated guardrail enforcement via `guardrails.py`.

## Configuration
See `config.example.json` for setup.
