thresholds:
  risk:
    max_reliability_risk_score: 2          # 0-5 scale
    max_security_risk_score: 1             # 0-5 scale
  finops:
    max_spend_delta_percent_per_change: 3.0
    max_monthly_spend_delta_percent: 7.0
  ci:
    required_checks_must_pass: true
    allow_reduced_coverage: false
  autonomy:
    confidence_min_for_automerge: 0.85
    confidence_min_for_autoapprove: 0.75
  rollback:
    required: true
    max_rollback_time_minutes: 30
  collection:
    max_contested_provider_share: 0.40
    api_drift_detect_minutes_max: 15
    api_drift_mitigate_minutes_max: 60
    min_priority_archive_completeness: 0.97
