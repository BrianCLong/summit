# Jules Agent

Jules is the primary builder/implementation agent. Existing prompts and guidance live elsewhere in the repo (for example `prompts/jules-gemini.md`). This directory is reserved for Summit-specific Jules runtime wiring alongside other agents.
