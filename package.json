{
  "name": "summit-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r --filter=./packages/* run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "format": "prettier --write .",
    "compliance:evidence": "echo 'Collecting compliance evidence...'",
    "ga:evidence": "pnpm compliance:evidence",
    "lint:release-policy": "node scripts/release/lint-reason-codes.mjs",
    "verify:runtime": "node scripts/verify-runtime.js"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "dependencies": {
    "entities": "4.5.0"
  }
}
