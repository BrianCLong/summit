{
  "name": "agent-workbench-backend",
  "version": "1.0.0",
  "description": "Minimal backend for the Agent Workbench UI",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build": "turbo run build --filter=...",
    "lint": "eslint .",
    "test": "jest",
    "validate:trajectory": "node scripts/run-trajectory-tests.js --input tests/trajectory",
    "validate:grounding": "node scripts/run-grounding-verifier.js --input tools/check-grounding/cases"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@changesets/cli": "^2.29.7",
    "fast-glob": "^3.3.2",
    "js-yaml": "^4.1.0",
    "xmlbuilder2": "^3.1.1"
  }
}
