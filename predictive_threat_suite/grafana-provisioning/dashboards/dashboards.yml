# Grafana Dashboard Provisioning Configuration

apiVersion: 1

providers:
  - name: 'Predictive Suite Dashboards'
    orgId: 1
    folder: 'Predictive Suite'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
