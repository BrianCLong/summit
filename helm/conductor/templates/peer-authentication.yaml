apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: conductor-mtls
  labels:
    app.kubernetes.io/name: conductor
spec:
  selector:
    matchLabels:
      app: conductor
  mtls:
    mode: STRICT
