apiVersion: v1
kind: Service
metadata:
  name: conductor-preview
  labels:
    app.kubernetes.io/name: conductor
spec:
  selector:
    app: conductor
  ports:
    - name: http
      port: 80
      targetPort: 4000
