{{- if .Values.createNamespace }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
  labels:
{{ toYaml (default dict .Values.defaults.namespaceLabels) | indent 4 }}
{{- end }}
