apiVersion: v1
kind: Service
metadata: { name: { { include "sandbox.fullname" . } } }
spec:
  selector: { app: { { include "sandbox.name" . } } }
  ports:
    - name: http
      port: { { .Values.service.port } }
      targetPort: 4003
