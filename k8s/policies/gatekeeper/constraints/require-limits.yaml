apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sRequiredLimits
metadata:
  name: require-limits
spec:
  match:
    kinds:
      - apiGroups: ['*']
        kinds: ['Deployment', 'StatefulSet']
  parameters:
    limits: ['cpu', 'memory']
