apiVersion: batch/v1
kind: CronJob
metadata: { name: purge-processor, namespace: prod }
spec:
  schedule: '0 1 * * *' # daily
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: processor
              image: ghcr.io/<org>/<repo>/dbtools:latest
              envFrom: [{ secretRef: { name: db-credentials } }]
              command: ['/bin/sh', '-c']
              args:
                - |
                  echo "Processing approved deletion requests...";
                  # TODO: replace with actual processing logic
                  echo OK
