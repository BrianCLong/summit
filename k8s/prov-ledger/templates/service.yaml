apiVersion: v1
kind: Service
metadata: { name: { { include "prov-ledger.fullname" . } } }
spec:
  selector: { app: { { include "prov-ledger.name" . } } }
  ports:
    - name: http
      port: { { .Values.service.port } }
      targetPort: 4001
