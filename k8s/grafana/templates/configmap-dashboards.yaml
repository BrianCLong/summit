apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
  labels: { grafana_dashboard: "1" }
data:
  intelgraph_slos.json: |
{{ (.Files.Get "../../ops/grafana/dashboards/intelgraph_slos.json") | indent 4 }}
