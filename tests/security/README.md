# Security Tests

This directory contains security regression tests and deny-by-default policy checks.

## Tests
* `test_network_deny_default.py`: Ensures sandbox blocks network/fs by default.

## Evidence
Pass/Fail results from these tests constitute evidence `EVD-PCTX_CODEMODE-SEC-002`.
