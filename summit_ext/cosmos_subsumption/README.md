# Cosmos Subsumption

This extension implements a secure-by-default gateway and identity management system for Summit, inspired by Cosmos Server.

## Features
- Policy-aware gateway (deny-by-default)
- Single identity plane (OIDC)
- Sudo/admin elevation mode
- App template importer with provenance checks
- Evidence-first security reporting
