syntax = "proto3";
package ig.wallets.v1;
service Wallets { rpc Verify(VerifyRequest) returns (VerifyResponse); }
message VerifyRequest { string wallet_id = 1; }
message VerifyResponse { bool is_valid = 1; }
