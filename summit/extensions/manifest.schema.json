{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "summit extension manifest",
  "type": "object",
  "required": ["name", "version", "capabilities"],
  "properties": {
    "name": { "type": "string" },
    "version": { "type": "string" },
    "capabilities": { "type": "array", "items": { "type": "string" } },
    "signature": { "type": "string", "description": "optional stub; required when enforcement enabled" }
  }
}
