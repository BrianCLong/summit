version: 1
enabled: true
default: deny
classes:
  command_decode: allow
  identity_inference: deny
  emotion_inference: deny
  physiology_inference: deny
logging:
  never_log_fields:
    - raw_signal
    - biometric_features
    - identity_vector
retention_days: 0
