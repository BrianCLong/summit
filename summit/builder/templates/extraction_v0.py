# summit/builder/templates/extraction_v0.py

# This file could contain template-specific configuration or logic
# that plan.py imports. For now, it's a marker.

TEMPLATE_ID = "extraction_v0"
DEFAULT_STEPS = ["classify", "extract", "validate"]
