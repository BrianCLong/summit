name: 'Video Pipeline'
description: 'Transcribe→caption→summary'
nodes:
  - id: asr
    type: tool
    params: { tool: 'whisper', model: 'large-v3' }
  - id: cap
    type: tool
    params: { tool: 'subtitle', style: 'srt' }
  - id: sum
    type: llm
    params: { provider: openai, model: gpt-4o-mini }
edges:
  - { from: asr, to: cap }
  - { from: cap, to: sum }
policies:
  - type: budget
    capUSD: 0.04
