{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV", "CI"],
  "globalDependencies": [
    "tsconfig.json",
    "tsconfig.base.json",
    ".eslintrc.*",
    "jest.config.*",
    "pnpm-lock.yaml",
    "workspaces/workspace-manifest.json"
  ],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", ".next/**", "out/**"],
      "inputs": [
        "src/**",
        "app/**",
        "public/**",
        "packages/**",
        "services/**",
        "workspaces/workspace-manifest.json",
        "package.json",
        "tsconfig.json",
        "tsconfig.base.json",
        "vite.config.ts",
        "vite.config.js",
        "index.html",
        "codegen.yml"
      ],
      "env": ["NODE_ENV"]
    },
    "lint": {
      "outputs": [],
      "cache": true,
      "inputs": [
        "src/**",
        "app/**",
        "services/**",
        "packages/**",
        "tests/**",
        "__tests__/**",
        ".eslintrc.*",
        "tsconfig.json",
        "package.json",
        "workspaces/workspace-manifest.json"
      ]
    },
    "typecheck": {
      "outputs": [],
      "cache": true,
      "inputs": [
        "src/**",
        "types/**",
        "packages/**",
        "services/**",
        "package.json",
        "tsconfig.json",
        "tsconfig.base.json",
        "workspaces/workspace-manifest.json"
      ]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": ["coverage/**"],
      "inputs": [
        "src/**",
        "tests/**",
        "__tests__/**",
        "package.json",
        "jest.config.*",
        "tsconfig.json",
        "workspaces/workspace-manifest.json"
      ],
      "env": ["NODE_ENV", "CI"]
    },
    "test:integration": {
      "dependsOn": ["build"],
      "outputs": ["test-results/**", "junit.xml", "playwright-report/**"],
      "cache": false,
      "env": ["NODE_ENV", "CI"]
    },
    "smoke": {
      "dependsOn": ["build"],
      "outputs": [],
      "cache": false
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
