name: vuln-allow-check

permissions:
  contents: read
on: [pull_request]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/vuln-allow.js
