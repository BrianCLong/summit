name: blueprint-scorecard

permissions:
  contents: read
on: [pull_request]
jobs:
  score:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout @v4/**
      - run: node tools/blueprints/score.ts packages/blueprints/node-service.yaml service-example
