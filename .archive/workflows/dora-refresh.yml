name: dora-refresh

permissions:
  contents: read
on:
  schedule: [{ cron: '*/30 * * * *' }]
jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node dora/update.js
        env: { GH_TOKEN: ${{ secrets.GH_TOKEN }} }