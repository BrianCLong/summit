version: '3.9'
services:
  summit-multi-agent:
    build:
      context: ..
      dockerfile: docker/Dockerfile.multi-agent
    network_mode: none
    read_only: true
    tmpfs:
      - /tmp
    volumes:
      - ../artifacts:/app/artifacts
    command: ["--task", "sample", "--out", "/app/artifacts"]
