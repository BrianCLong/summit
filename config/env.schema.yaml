required:
  - PORT
  - LOG_LEVEL
  - OTEL_EXPORTER_OTLP_ENDPOINT
  - MAESTRO_API_TOKEN
properties:
  PORT: { type: number, default: 8080 }
  LOG_LEVEL: { type: string, enum: [debug, info, warn, error], default: info }
  OTEL_EXPORTER_OTLP_ENDPOINT: { type: string }
  MAESTRO_API_TOKEN: { type: string, secret: true }
