{
  "annotations": {
    "list": []
  },
  "editable": true,
  "gnetId": null,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "title": "Global Health Status",
      "type": "stat",
      "datasource": "Prometheus",
      "gridPos": { "h": 4, "w": 24, "x": 0, "y": 0 },
      "targets": [
        {
          "expr": "sum(up{job=\"api-server\"}) by (region)",
          "legendFormat": "{{region}}"
        }
      ]
    },
    {
      "title": "Cross-Region Latency (p95)",
      "type": "timeseries",
      "datasource": "Prometheus",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 4 },
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{}[5m])) by (le, region))",
          "legendFormat": "{{region}}"
        }
      ]
    },
    {
      "title": "Database Replication Lag",
      "type": "timeseries",
      "datasource": "CloudWatch",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 4 },
      "targets": [
        {
          "namespace": "AWS/RDS",
          "metricName": "AuroraGlobalDBReplicationLag",
          "region": "us-east-1",
          "statistics": ["Maximum"]
        }
      ],
      "thresholds": [
        {
          "value": 1000,
          "color": "red"
        }
      ]
    },
    {
      "title": "Active Sessions by Region",
      "type": "gauge",
      "datasource": "Prometheus",
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 12 },
      "targets": [
        {
          "expr": "sum(active_sessions) by (region)",
          "legendFormat": "{{region}}"
        }
      ]
    }
  ],
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["dr", "multi-region", "ops"],
  "templating": {
    "list": []
  },
  "time": {
    "from": "now-6h",
    "to": "now"
  },
  "timepicker": {},
  "timezone": "",
  "title": "Multi-Region DR & Health"
}
