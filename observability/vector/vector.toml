[api]
enabled = true
address = "0.0.0.0:8686"

[sources.docker]
type = "docker_logs"

[sinks.loki]
type = "loki"
inputs = ["docker"]
endpoint = "http://loki:3100"
encoding.codec = "json"
labels.source = "docker"
