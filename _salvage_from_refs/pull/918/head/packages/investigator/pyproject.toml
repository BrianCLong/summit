[project]
name = "investigator"
version = "0.1.0"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic",
]
[project.optional-dependencies]
test = [
  "pytest",
  "httpx",
]
[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
