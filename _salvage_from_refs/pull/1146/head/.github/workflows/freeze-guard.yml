name: freeze-guard

permissions:
  contents: read
on: [workflow_call]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/error_budget_check.js