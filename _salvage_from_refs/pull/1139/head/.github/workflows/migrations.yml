name: Migrations Safety
on: [pull_request]
jobs:
  lint-dryrun:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout @v4/**
      - run: node tools/db/migration_lint.ts
      - run: node tools/db/dryrun.ts
        env: { SHADOW_DB_URL: ${{ secrets.SHADOW_DB_URL }} }