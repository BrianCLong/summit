name: flags-audit
on:
  schedule: [{ cron: '0 8 * * 1' }]
  pull_request:
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/flags-audit.ts