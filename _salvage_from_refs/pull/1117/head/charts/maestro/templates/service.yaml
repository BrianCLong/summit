apiVersion: v1
kind: Service
metadata:
  name: { { include "maestro.fullname" . } }
spec:
  type: { { .Values.service.type } }
  selector:
    app: { { include "maestro.name" . } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: { { .Values.service.targetPort } }
