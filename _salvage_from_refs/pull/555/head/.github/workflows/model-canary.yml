name: Model Canary

on:
  workflow_dispatch:

jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Shadow evaluate model
        run: npm test
      - name: Promote model
        run: scripts/ml/promote_model.sh model.tar
