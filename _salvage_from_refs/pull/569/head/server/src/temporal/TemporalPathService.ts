export class TemporalPathService {
  async kShortestPaths(session: any, from: string, to: string, k = 3, since?: number, until?: number) {
    // Placeholder implementation
    return [];
  }
}

export default TemporalPathService;
