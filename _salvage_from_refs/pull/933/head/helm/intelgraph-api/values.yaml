replicaCount: 3
image:
  repository: ghcr.io/your-org/intelgraph-api
  tag: "v0.1.0"
  pullPolicy: IfNotPresent

resources: {}
env: []

rollouts:
  enabled: true

prometheus:
  address: "http://prometheus.monitoring.svc.cluster.local:9090"

ingress:
  enabled: true
  className: nginx
  host: api.example.com
  # If using NGINX canary via header or weight, add the needed annotations in your Ingress manifests.
