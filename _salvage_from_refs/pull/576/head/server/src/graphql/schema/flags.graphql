type FlagDecision {
  key: String!
  value: String!
  reason: String!
  at: String!
}

type Query {
  flag(key: String!): FlagDecision
}

type Mutation {
  setFlag(key: String!, value: String!): FlagDecision!
}
