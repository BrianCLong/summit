[project]
name = "graphai-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "celery",
  "redis",
  "requests"
]
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
