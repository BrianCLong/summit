[project]
name = "intelgraph-er"
version = "0.1.0"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic",
  "scikit-learn",
  "numpy",
  "pandas"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
