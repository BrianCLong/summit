# Stats queries using actual server schema

query GW_ServerStatsQuery {
  serverStats {
    uptime
    totalInvestigations
    totalEntities
    totalRelationships
    databaseStatus {
      redis
      postgres
      neo4j
    }
  }
}

query GW_HealthCheck {
  health
}

