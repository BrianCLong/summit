[project]
name = "prov-ledger"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "networkx",
    "prometheus-client",
    "cryptography",
]

[project.optional-dependencies]
test = ["pytest", "httpx"]

[project.scripts]
ig-prov-verify = "app.cli:main"
