mutation setCanaryWeights($tenant: Tenant!, $weights: CanaryWeightsInput!) {
  setCanaryWeights(tenant: $tenant, weights: $weights) {
    ok
    audit {
      evidenceId
      ts
      actor
    }
    warnings
  }
}
