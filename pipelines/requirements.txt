# Summit Pipeline Orchestration Requirements

# Core dependencies
pyyaml==6.0.3
jsonschema==4.26.0

# OpenLineage integration
# openlineage-python>=0.20.0  # Uncomment to enable
requests==2.32.5

# Testing
pytest==9.0.2
pytest==9.0.2

# Optional: Airflow for DAG generation testing
# apache-airflow>=2.5.0  # Only needed if testing Airflow DAG generation
