1. Add the prompt text exactly as provided to `prompts/jules_workspace_orchestrator.txt`.
2. Generate its SHA-256 hash.
3. Add the prompt to `prompts/registry.yaml` with the hash.
4. Complete pre-commit steps.
5. Submit.
