# For local-only development
# Use these settings to run everything in Docker.
NODE_ENV=development

# PostgreSQL
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=ig
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Neo4j
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=neo4jpass

# Redis
REDIS_URL=redis://redis:6379

# --- Hybrid (Local + AWS) Configuration ---
# Uncomment and replace with your AWS resource details to connect your local
# API service to managed AWS data stores.

# AWS Region
# AWS_REGION=us-east-1

# AWS RDS PostgreSQL
# DB_HOST=<your-rds-endpoint>.rds.amazonaws.com
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=<your-db-password-from-secrets-manager>
# DB_NAME=ig
# DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# AWS Neo4j (if on EC2/EKS)
# NEO4J_URI=bolt://<your-neo4j-endpoint>:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=<your-neo4j-password-from-secrets-manager>

# AWS ElastiCache Redis
# REDIS_URL=redis://<your-elasticache-endpoint>:6379