* text=auto eol=lf
*.sh text eol=lf
*.bat text eol=crlf

# Lockfiles – do not noise diffs
package-lock.json -diff
yarn.lock -diff
pnpm-lock.yaml -diff
**/package-lock.json -diff
**/yarn.lock -diff
**/pnpm-lock.yaml -diff

# Ensure VRF fixtures remain reviewable text
*.hex text eol=lf

# Graph schemas
*.graphql diff
*.cql diff

# LFS
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text
