* text=auto eol=lf
*.sh text eol=lf
*.bat text eol=crlf

# Lockfjes must remain diffable for CODEX PRs
package-lock.json text diff
yarn.lock text diff
pnpm-lock.yaml text diff
poetry.lock text diff
composer.lock text diff
Cargo.lock text diff

go.sum text diff

* -text
