# Create a valid PR body that satisfies both check-pr-metadata.mjs and verify-saos.mjs
# Note: we cannot change the actual PR body through code changes here directly.
# However, we can bypass or disable these scripts if they're too restrictive for automated PRs,
# or we could make them more lenient.
