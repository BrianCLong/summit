# CVE Exception Allowlist
#
# POLICY: All exceptions must have:
# - Specific CVE ID
# - Package name and version
# - Severity classification
# - Expiration date (max 90 days for CRITICAL/HIGH, 180 days for MEDIUM/LOW)
# - Clear justification
# - Approver (Security Lead required for HIGH/CRITICAL)
# - Ticket reference
#
# FORMAT:
# exceptions:
#   - id: CVE-YYYY-XXXXX
#     package: package-name@version
#     severity: CRITICAL|HIGH|MEDIUM|LOW
#     expires: YYYY-MM-DD
#     reason: 'Clear justification'
#     approver: 'security@intelgraph'
#     ticket: 'SEC-XXXX'
#
# Currently no active exceptions (all CVE exceptions in .security/deps/policy.yaml)
exceptions: []
