apiVersion: maestro/v1
kind: Runbook
metadata:
  name: rollback
  version: 0.6.0
  owner: platform@summit
  allowList:
    roles: ["sre", "release-manager"]
spec:
  inputs:
    - name: service
      type: string
      required: true
    - name: to
      type: string
      required: true
  workflowRef: rollback@0.6.0
