name: Release

on:
  push:
    tags: [ 'v*.*.*' ]

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  test:
    uses: ./.github/workflows/contract-tests.yml

  build-and-publish:
    needs: test
    uses: ./.github/workflows/build-publish.yml
