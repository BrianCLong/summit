# ETL Assistant Python dependencies

# Core dependencies
httpx==0.28.1
pytest==9.0.2
pytest==9.0.2

# Optional dependencies (already available in main project)
# rapidfuzz>=3.0.0  # Fuzzy matching (used in universal-ingest)
# defusedxml>=0.7.1  # Safe XML parsing (used in universal-ingest)
