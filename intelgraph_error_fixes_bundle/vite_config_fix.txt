
// PATCH: Correct Vite config and exclude Playwright from Vitest

// Example vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    exclude: ['tests/e2e/**']
  }
});
