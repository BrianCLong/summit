{
  "name": "ingestion-contracts",
  "version": "4.2.3",
  "description": "Contract specification and certification toolkit for ingestion pipeline",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/**/*.js",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
