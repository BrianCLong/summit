#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

LOG_FILE="git-hook.log"

log() {
  echo "[$(date -u +"%Y-%m-%dT%H:%M:%SZ")] $1" >> "$LOG_FILE"
  echo "$1"
}

log "🔒 Running pre-commit checks..."

# Run gitleaks
if command -v gitleaks >/dev/null 2>&1; then
  log "🔍 Scanning for secrets..."
  gitleaks protect --staged --verbose || {
    log "❌ Secret detected!"
    exit 1
  }
fi

# Run lint-staged
log "🔍 Running lint-staged..."
pnpm exec lint-staged || {
  log "❌ Lint-staged failed."
  exit 1
}

# Run screenshots (robust)
log "📸 Generating screenshots..."
CMD="npx tsx server/scripts/generate-screenshots.ts"

# Use timeout if available, otherwise rely on script's internal timeout
if command -v timeout >/dev/null 2>&1; then
  timeout 35s $CMD >> "$LOG_FILE" 2>&1 || log "⚠️ Screenshot generation failed (check logs)"
else
  $CMD >> "$LOG_FILE" 2>&1 || log "⚠️ Screenshot generation failed (check logs)"
fi

log "✅ All checks passed!"
