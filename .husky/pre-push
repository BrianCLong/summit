#!/usr/bin/env sh
set -e

# Husky v9-style: no _/husky.sh, skip quietly in limited PATH envs
command -v node >/dev/null 2>&1 || exit 0
npx --yes jest --version >/dev/null 2>&1 || exit 0

# Run CI-style tests (configured in package.json)
npm run -s test:ci
