{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "evidence_id": { "type": "string" },
    "benchmark_id": { "type": "string" },
    "run_id": { "type": "string" },
    "metrics": { "type": "object" }
  },
  "anyOf": [
    { "required": ["evidence_id", "metrics"] },
    { "required": ["benchmark_id", "run_id", "metrics"] }
  ],
  "additionalProperties": true
}
