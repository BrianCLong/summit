{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Summit Evidence Pack V1 - Index",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["evidence_id", "area", "description", "files"],
    "properties": {
      "evidence_id": {
        "type": "string",
        "description": "Unique identifier for the evidence artifact."
      },
      "area": {
        "type": "string",
        "description": "Functional area or domain the evidence belongs to."
      },
      "description": {
        "type": "string",
        "description": "Human-readable description of what this evidence proves."
      },
      "files": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "List of relative file paths included in this evidence bundle."
      }
    }
  }
}
