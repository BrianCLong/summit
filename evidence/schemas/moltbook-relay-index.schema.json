{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Moltbook Relay Evidence Index",
  "type": "object",
  "required": ["version", "map"],
  "properties": {
    "version": { "type": "string" },
    "map": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["report", "metrics", "stamp"],
        "properties": {
          "report": { "type": "string" },
          "metrics": { "type": "string" },
          "stamp": { "type": "string" }
        }
      }
    }
  },
  "additionalProperties": true
}
