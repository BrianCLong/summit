apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: satellite-egress
spec:
  podSelector:
    matchLabels:
      app: satellite
  policyTypes: [Egress]
  egress:
    - to: []
      ports:
        - protocol: TCP
          port: 443
