{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "summit.fullname" . }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: {{ include "summit.fullname" . }}
  policyTypes: [ Egress ]
  egress:
    {{- toYaml .Values.networkPolicy.egress | nindent 2 }}
{{- end }}
