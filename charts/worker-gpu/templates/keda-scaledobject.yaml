apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: worker-gpu-scaledobject
spec:
  scaleTargetRef:
    name: worker-gpu
  cooldownPeriod: 60
  minReplicaCount: 0
  maxReplicaCount: 10
  triggers:
    - type: cpu
      metadata:
        type: Utilization
        value: '70'
