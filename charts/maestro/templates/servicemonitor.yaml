apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: maestro
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: maestro
  endpoints:
    - path: /metrics
      port: http
      interval: 15s
