{
  "name": "@intelgraph/gateway",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p . || true",
    "dev": "node --watch dist/server.js || ts-node src/server.ts",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "express": "^4.19.2",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
