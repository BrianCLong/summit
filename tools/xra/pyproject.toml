[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "xra"
version = "0.1.0"
description = "Explainable Ranking Auditor toolkit"
authors = [
  {name = "Summit AI"}
]
license = {text = "MIT"}
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.scripts]
xra = "xra.cli:main"
