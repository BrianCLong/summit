name: SecretSentry (warn)

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  secretsentry:
    name: Secret scan (warn)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install SecretSentry
        run: |
          pip install --upgrade pip
          pip install ./tools/secretsentry
      - name: Run SecretSentry in warn mode
        run: |
          secretsentry scan --path . --allowlist .secretsentryignore --mode warn \
            --json-report secretsentry-report.json --markdown-report secretsentry-report.md
      - name: Upload SecretSentry report
        uses: actions/upload-artifact@v4
        with:
          name: secretsentry-report-warn
          path: |
            secretsentry-report.json
            secretsentry-report.md
