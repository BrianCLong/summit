apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: graph-xai
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: graph-xai
                port:
                  number: 8090
