{
  "name": "summit-mini",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm -C server dev & pnpm -C web dev",
    "build": "pnpm -C server build && pnpm -C web build",
    "start": "pnpm -C server start"
  },
  "pnpm": {
    "overrides": {
      "diff": "4.0.4",
      "esbuild": "^0.25.0"
    }
  },
  "version": "4.2.3"
}
