processors:
  attributes/scrub:
    actions:
      - key: http.request.header.authorization
        action: delete
      - key: user.email
        action: delete
exporters: { /* ... */ }
service:
  pipelines:
    traces:
      processors: [batch, attributes/scrub]