{
  "name": "json",
  "description": "JSON and document-store schemas used by services.",
  "schemaGlobs": [
    "migrations/json/**/*.{json,yaml,yml,ts}",
    "schemas/json/**/*.{json,yaml,yml}",
    "packages/**/*document-schema*/**/*.{json,yaml,yml,ts}"
  ],
  "migrationGlobs": ["migrations/json/**/*.{json,yaml,yml,ts}"]
}
