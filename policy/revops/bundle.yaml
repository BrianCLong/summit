name: revops
version: revops-2025.02.15
includes:
  - schemas/input/*.json
  - schemas/decision/*.json
  - rego/*.rego
  - tests/*.rego
meta:
  owner: revops-domain-team
  slos:
    decision_latency_ms_p95: 100
