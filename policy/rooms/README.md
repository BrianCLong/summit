# Controlled Multi-Agent Exchange (CMAE) rooms

Rooms are *not* enabled by default.
Runtime MUST enforce:
- SUMMIT_CMAE_ENABLED must be true
- room id must be in allowlist

This directory is data-only scaffolding to minimize blast radius.
