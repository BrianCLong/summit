policy:
  module: event_playbooks
  mode: analytics_only
  deny:
    - action: "generate_counter_message"
    - action: "recommend_amplification"
    - action: "identify_persuadable_individuals"
  allow:
    - action: "compute_aggregate_metrics"
    - action: "emit_evidence_bundle"
