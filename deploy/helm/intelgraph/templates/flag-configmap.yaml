apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "intelgraph.fullname" . }}-flags
  labels:
    app.kubernetes.io/component: feature-flags
    app.kubernetes.io/name: {{ include "intelgraph.name" . }}
data:
  targets.yaml: |
{{ (.Files.Get (printf "flags/targets/%s.yaml" (.Values.global.environment | default "dev"))) | nindent 4 }}
