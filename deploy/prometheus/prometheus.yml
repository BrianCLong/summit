global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "maestro-server"
    metrics_path: /metrics
    static_configs:
      - targets: ["server:4000"]
rule_files:
  - /etc/prometheus/alerts/*.yaml
