# Values for policy-controller helm chart
# repo: https://sigstore.github.io/helm-charts
# chart: policy-controller

installCRDs: true

webhook:
  enabled: true
  failurePolicy: Fail # Enforce strictly

# Enable if using cosign keyless
cosign:
  secretKeyRef: {} # Keyless
