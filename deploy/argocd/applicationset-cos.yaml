apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: companyos
spec:
  generators:
    - list:
        elements:
          - env: dev
          - env: stage
          - env: prod
  template:
    metadata:
      name: cos-{{env}}
    spec:
      project: default
      source:
        repoURL: https://github.com/intelgraph/summit
        targetRevision: HEAD
        path: charts/companyos
        helm:
          valueFiles:
            - values/{{env}}.yaml
      destination:
        server: https://kubernetes.default.svc
        namespace: cos-{{env}}
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true

