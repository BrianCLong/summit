version: '3.8'
services:
  graphql-orchestrator:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - OPA_URL=${OPA_URL}
  bandit-router:
    build: .
  web-fetch-worker:
    build: .
  synthesizer:
    build: .
