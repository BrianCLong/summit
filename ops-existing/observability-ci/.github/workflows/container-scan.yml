name: container-scan
on:
  workflow_call:
    inputs:
      image:
        required: true
        type: string
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: aquasecurity/trivy-action@v0.12.0
        with:
          image-ref: ${{ inputs.image }}
          format: table
