# Latency Regression Detection Harness

This harness flags performance regressions in Summit's GraphRAG pipeline by running benchmark queries, recording per-stage latency breakdowns, and comparing against a stored baseline.
