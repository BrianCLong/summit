# PPPT / Product

Goal: turn customer-language interviews into a crisp USP + GTM message.

Workflow:
1. Capture 5+ interviews using `customer_interview.schema.json`.
2. Synthesize into `gtm_message.template.json`.
3. Attach evidence references in `evidence/index.json`.
