apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: prov-ledger
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: prov-ledger
  minReplicas: 1
  maxReplicas: 3
  metrics: []
