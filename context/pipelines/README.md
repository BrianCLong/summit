# Context Pipelines

This directory contains deterministic pipeline definitions for context engineering.

## Contract
1. **Determinism**: All pipelines must produce the same output hash for the same input.
2. **Budgets**: `budget_tokens` is a hard limit.
3. **Hashing**: `hash.algorithm` defines the integrity check.
