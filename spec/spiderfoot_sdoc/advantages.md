# Selective-Disclosure OSINT Capsules (SDOC) — Advantages

- Replaces “dump raw scan results” with **selective disclosure** (minimal necessary outputs, aggregation, redaction).
- Enforces **sensitivity budgets** and TTL retention—enterprise privacy/compliance by design.
- Produces verifiable capsules with scan ledger + witness records for audit.
