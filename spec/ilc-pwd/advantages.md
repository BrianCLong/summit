# Advantages over Prior Art — ILC-PWD

- Converts intelligence scores into an explicit lifecycle state machine with provable transitions justified by a minimal support set.
- Applies provenance-weighted decay that differentiates evidence classes and freshness, improving stability and explainability.
- Supports counterfactual remediation impacts on lifecycle state, enabling actionable decisioning rather than opaque scoring.
