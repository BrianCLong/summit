# ABI Schema

**Outputs**

- Typed schemas for object fields (validation rules, cardinality, effect typing).
- Client stubs for orchestration tools and audit UIs.
- Determinism token binding schema versions.

**Contracts**

- Preconditions and postconditions expressed in policy-as-code (e.g., Rego).
- Effect signatures: read, write, export with purpose binding.
