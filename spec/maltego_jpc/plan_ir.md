# JPC Plan IR

## Structure

- Nodes represent transforms and intermediate entities.
- Edges represent dataflow and join operations.
- Attributes include join keys, source identifiers, and filters.

## Requirements

- Canonical ordering for deterministic compilation.
- Explicit provenance links for each intermediate entity.
