# Advantages — JPC

- Automatically reduces transform graphs into minimal source query sets while preserving join semantics.
- Improves cost and latency by batching and eliminating redundant queries across sources.
- Produces a proof-like join preservation certificate and witness chain for audits.
