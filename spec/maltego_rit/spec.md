# RIT: Result Inversion for Transforms

RIT infers a minimal generating transform subplan and minimal input set that could produce a given graph result set, enabling explainability, reproducibility, and rollback.
