# Module Metadata Schema

- Module identifier, version, jurisdiction rules, ToS constraints.
- Effect signature (passive/active, export/read/write).
- Risk score and required authorization levels.
- Rate-limit contracts (per-target, per-tenant).
