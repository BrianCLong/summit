# Advantages — MRSGS

- **Spectral fidelity across scales**: captures campaign structure at coarse
  and fine resolutions, outperforming single-scale community heuristics.
- **Streaming efficiency**: bounded-memory sketch updates support real-time
  detection in high-volume event streams.
- **Audit-ready artifacts**: emits deterministic campaign signatures with
  explanation subgraphs, Merkle commitments, and optional attestation.
- **Baseline-aware**: leverages reference libraries keyed by topic and region to
  reduce false positives caused by normal cultural patterns.
