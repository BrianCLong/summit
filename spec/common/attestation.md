# Attestation Requirements

Defines hardware or software attestation hooks for sensitive operations.

## Requirements

- Support TEE quotes bound to capsule or artifact digests.
- Capture signer identity and certificate chain for audit.
- Expose attestation reference within replay tokens and witness chains.
- Provide verification workflow for clients and auditors.
