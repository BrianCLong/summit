# Transparency Log Interface

Specifies publication requirements for artifacts and witness hashes.

## Capabilities

- Append-only entries containing artifact commitments and policy decisions.
- Inclusion proofs using Merkle trees for external auditors.
- API for verifying replay tokens against published entries.
- Retention policies aligned with sensitivity budgets.
