# Egress Receipts

Captures egress summaries for multi-performer executions.

## Fields

- Execution identifier and scope token reference.
- Egress summaries (destination, byte count, content class) with threshold status.
- Halt reasons when thresholds exceeded.
- Hash commitment to logs and outputs tied to receipt.
