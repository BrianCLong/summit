# Advantages — TRACER

- Produces role attributions using constrained temporal routing rather than simple propagation counts.
- Outputs role certificates with minimal support subgraphs and replay tokens for audit and replay.
- Enables downstream automation by gating actions on role confidence (e.g., bridge accounts prioritized for disruption).
