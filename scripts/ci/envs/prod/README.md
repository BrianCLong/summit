# Prod Environment Parity Inputs

This directory is intentionally constrained to hold production IaC/config inputs
for parity audits. Populate with environment-specific Terraform/Helm manifests
for release-train drift checks.
