#!/usr/bin/env node
/**
 * Drift Detector (skeleton)
 * - deterministic output: report.json, metrics.json, stamp.json
 * TODO: implement file presence + index checks; reuse verifier core.
 */
console.log("TODO: item-unknown drift detector");
process.exit(0);
