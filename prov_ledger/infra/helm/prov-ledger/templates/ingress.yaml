apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prov-ledger
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: prov-ledger
                port:
                  number: 80
