# WOW Demo Datasets

This folder contains zero-config demo datasets used by `pnpm demo:company`.

- `mit-sloan-startups-2026.jsonl`: startup entities and source-linked signals.
- `intsum-2026-threat-horizon.jsonl`: risk horizon context for multi-hop GraphRAG + swarm analysis.

These fixtures are designed for deterministic local demos and docs examples.
