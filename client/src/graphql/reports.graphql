# Mock report templates using server stats
query MockReportTemplates {
  serverStats {
    uptime
    totalInvestigations
  }
}

# Mock export using server stats
query MockExportData($investigationId: ID!) {
  serverStats {
    uptime
    # Mock export data - would be replaced with real export functionality
  }
}