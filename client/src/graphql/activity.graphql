query GW_ActivityFeedData($limit: Int, $offset: Int) {
  activities(limit: $limit, offset: $offset) {
    id
    actionType
    resourceType
    resourceId
    actorId
    timestamp
    payload
    metadata
  }
}

# Mock comment operations (kept for compatibility if needed, or remove)
query GW_MockComments($targetId: ID!) {
  serverStats {
    uptime
  }
}
