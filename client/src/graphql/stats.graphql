# Stats queries using actual server schema

query ServerStatsQuery {
  serverStats {
    uptime
    totalInvestigations
    totalEntities
    totalRelationships
    databaseStatus {
      redis
      postgres
      neo4j
    }
  }
}

query HealthCheck {
  health
}

