# Timeline queries using actual server schema

query GW_TimelineEvents($investigationId: ID!, $startDate: String, $endDate: String) {
  timelineEvents(investigationId: $investigationId, startDate: $startDate, endDate: $endDate) {
    id
    timestamp
    eventType
    entityId
    description
    severity
    source
    metadata
  }
}