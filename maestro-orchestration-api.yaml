paths:
  /fastlane/promote:
    post:
      summary: Trigger auto‑promotion if gates pass
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                runId: { type: string }
                manifestHash: { type: string }
      responses:
        "200": { description: Promotion accepted }
  /friction/alerts:
    post:
      summary: Create friction alert event
      responses:
        "202": { description: Accepted }