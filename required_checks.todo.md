# Required checks discovery (rename plan)
1) GitHub UI: Repo → Settings → Branches → Branch protection rule → “Require status checks”.
2) Copy exact check names here.
3) Rename temporary gates in `.github/workflows/*` to match.
