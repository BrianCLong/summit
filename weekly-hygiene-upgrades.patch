--- .github/workflows/weekly-hygiene.yml.orig
+++ .github/workflows/weekly-hygiene.yml
@@ -1,5 +1,9 @@
 name: Weekly Repository Hygiene

+concurrency:
+  group: weekly-hygiene
+  cancel-in-progress: false
+
 on:
   schedule:
     - cron: '0 0 * * MON'  # Every Monday at midnight UTC
@@ -224,3 +228,20 @@
           echo "- Report saved to: \`.repo_hygiene/report.json\`" >> $GITHUB_STEP_SUMMARY
           echo "- Audit branch: \`repo-hygiene/weekly\`" >> $GITHUB_STEP_SUMMARY
           echo "- Workflow run: [\`${{ github.run_id }}\`](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> $GITHUB_STEP_SUMMARY
+
+      - name: Notify completion (optional)
+        if: always()
+        run: |
+          echo "🔔 Weekly hygiene completed"
+
+          STATUS="${{ job.status }}"
+          PROCESSED_COUNT=$(echo "$processed_prs" | tr ',' '\n' | grep -c . || echo 0)
+          CONFLICT_COUNT=$(echo "$conflicted_prs" | tr ',' '\n' | grep -c . || echo 0)
+
+          # Optional: Add Slack/Teams webhook here
+          # curl -X POST "$SLACK_WEBHOOK" -H "Content-Type: application/json" \
+          #   -d "{\"text\":\"🧹 Weekly Hygiene $STATUS: $PROCESSED_COUNT processed, $CONFLICT_COUNT conflicts. <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View Run>\"}"
+
+          echo "Summary: $STATUS with $PROCESSED_COUNT processed PRs, $CONFLICT_COUNT conflicts"
+          echo "Run URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
+