apiVersion: v1
kind: Service
metadata:
  name: deescalation-coach
spec:
  selector:
    app: deescalation-coach
  ports:
    - port: { { .Values.service.port } }
      targetPort: { { .Values.service.port } }
